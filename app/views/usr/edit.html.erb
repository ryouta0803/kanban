<div class="userditPage">
  <div class="container">
    <%= form_with model: @user, url: { action: :update }, html: { class: 'userForm' }, local: true do |f| %>
      <% if @user.errors.any? %>
        <p class="text-danger">タイトルは1～255文字以内で入力してください</p>
      <% end %>
      <%= f.label :title %>
      <%= f.text_field :title, class: "form-control listName", placeholder: "リスト名" %>
      <div class="text-center"><%= f.submit "編集する", class: "submitBtn" %></div>
    <% end %>
  </div>
</div>
